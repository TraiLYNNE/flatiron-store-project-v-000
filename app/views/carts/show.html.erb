<h1>Your Cart</h1>

<% if notice %>
  <p><%= notice %></p>
<% end %>

<%= render partial: 'carts/cart', locals: {cart: @cart} %>

<p>Order total: <strong><%= number_to_currency(@cart.total/100) %></strong> </p>

<%= button_to('Checkout', checkout_path) if @cart.status == 'pending' %>
